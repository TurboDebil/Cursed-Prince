#ifndef H_UTILITY
#define H_UTILITY

#include <vector>

class Utility {
public:
  static int RandomGenerator(int min_value, int max_value);

  static int ProtectionFromDebil(int max_value);
};

#endif